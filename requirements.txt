# StreamChat Overlay — Python Dependencies
# Install with: pip install -r requirements.txt
#
# Python 3.14 compatibility notes:
#   - aiohttp is REMOVED: its greenlet dependency doesn't build on 3.14.
#     Replaced with httpx (pure Python, zero C extensions).
#   - uvicorn[standard] is REPLACED with plain uvicorn:
#     the [standard] extra pulls in httptools + uvloop which also fail on 3.14.
#   - All other packages either ship pre-built wheels for 3.14 or are pure Python.

# ── Core web framework ────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.29.0          # plain — no [standard] extra (avoids httptools/uvloop)
python-multipart==0.0.9

# ── Async HTTP (replaces aiohttp — pure Python, no C extensions) ─────────────
httpx==0.27.0

# ── WebSockets ────────────────────────────────────────────────────────────────
websockets==12.0

# ── YouTube Live Chat ─────────────────────────────────────────────────────────
pytchat==0.5.5

# ── HTML parsing (Rumble scraping) ────────────────────────────────────────────
beautifulsoup4==4.12.3
lxml==5.2.2

# ── Templating ────────────────────────────────────────────────────────────────
jinja2==3.1.4

# ── Static files ──────────────────────────────────────────────────────────────
aiofiles==23.2.1

# ── Environment variables ─────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Instagram Live (browser automation) ──────────────────────────────────────
# After pip install, also run: playwright install chromium
playwright==1.44.0
